<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IslamicAlarm">

        <!-- Main Alarm List Activity -->
        <activity
            android:name="com.hafij.islamicalarm.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.IslamicAlarm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fullscreen Lock Screen Activity -->
        <activity
            android:name="com.hafij.islamicalarm.LockScreenActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:showForAllUsers="true"
            android:theme="@style/Theme.IslamicAlarm.LockScreen" />

        <!-- Quran Activity -->
        <activity
            android:name="com.hafij.islamicalarm.quran.QuranActivity"
            android:exported="false"
            android:label="আল-কুরআন"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Tasbih Activity -->
        <activity
            android:name="com.hafij.islamicalarm.tasbih.TasbihActivity"
            android:exported="false"
            android:label="ডিজিটাল তাসবিহ"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Masnoon Duas Activity -->
        <activity
            android:name="com.hafij.islamicalarm.duas.DuasActivity"
            android:exported="false"
            android:label="মাসনুন দোআ ও হাদিস"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Ramadan & Fasting Activity -->
        <activity
            android:name="com.hafij.islamicalarm.ramadan.RamadanActivity"
            android:exported="false"
            android:label="রমজান ও রোজা"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- 99 Names of Allah Activity -->
        <activity
            android:name="com.hafij.islamicalarm.names.AllahNamesActivity"
            android:exported="false"
            android:label="আসমাউল হুসনা"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Daily Salah & Amal Tracker Activity -->
        <activity
            android:name="com.hafij.islamicalarm.amal.AmalTrackerActivity"
            android:exported="false"
            android:label="দৈনিক আমল ট্র্যাকার"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Tahajjud Calculator Activity -->
        <activity
            android:name="com.hafij.islamicalarm.tahajjud.TahajjudCalculatorActivity"
            android:exported="false"
            android:label="তাহাজ্জুদ ক্যালকুলেটর"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Hijri Calendar Activity -->
        <activity
            android:name="com.hafij.islamicalarm.prayertimes.HijriCalendarActivity"
            android:exported="false"
            android:label="হিজরি ক্যালেন্ডার"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Islamic Library Activity -->
        <activity
            android:name="com.hafij.islamicalarm.books.IslamicLibraryActivity"
            android:exported="false"
            android:label="ইসলামিক কিতাব ও হাদিস লাইব্রেরি"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Book Reader Activity -->
        <activity
            android:name="com.hafij.islamicalarm.books.BookReaderActivity"
            android:exported="false"
            android:label="কিতাব পাঠ"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Audio Azan Activity -->
        <activity
            android:name="com.hafij.islamicalarm.audio.AzanAudioActivity"
            android:exported="false"
            android:label="অডিও আজান"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- Full Audio Quran Activity -->
        <activity
            android:name="com.hafij.islamicalarm.audio.AudioQuranActivity"
            android:exported="false"
            android:label="অডিও কুরআন"
            android:theme="@style/Theme.IslamicAlarm" />

        <!-- App Widget Provider -->
        <receiver
            android:name="com.hafij.islamicalarm.widget.IslamicAlarmWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.hafij.islamicalarm.ACTION_REFRESH_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- Alarm Receiver -->
        <receiver
            android:name="com.hafij.islamicalarm.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Device Boot Receiver -->
        <receiver
            android:name="com.hafij.islamicalarm.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Phone Call State Receiver -->
        <receiver
            android:name="com.hafij.islamicalarm.CallReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
